#!/usr/bin/env sh
# SPDX-License-Identifier: GPL-3.0-or-later
# Check the commit message using the same contract as the shared CI action.

. "$(dirname "$0")/hook-enabled.sh"
hook_enabled noAttribution true || exit 0

exec node "$(dirname "$0")/commit-message.mjs" "$1"
