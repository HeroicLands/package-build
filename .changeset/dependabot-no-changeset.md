---
---

Dependabot pull requests no longer have to declare a changeset. A dependency bump
here is always a `devDependency` and reaches no consumer, so there is no release
to declare and no changelog line to write.
